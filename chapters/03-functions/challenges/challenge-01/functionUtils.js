/**
 * 综合挑战 01：函数工具库
 *
 * 实现一组实用的函数工具
 */

// ============================================
// 1. 函数管道
// ============================================

/**
 * 创建函数管道，从左到右依次执行
 * @param {...Function} fns - 要组合的函数
 * @returns {Function} 组合后的函数
 */
export function pipe(...fns) {
  // TODO: 实现管道
  // pipe(f, g, h)(x) === h(g(f(x)))
}

// ============================================
// 2. 通用柯里化
// ============================================

/**
 * 将函数柯里化
 * @param {Function} fn - 要柯里化的函数
 * @returns {Function} 柯里化后的函数
 */
export function curry(fn) {
  // TODO: 实现通用柯里化
  // 支持任意参数数量的函数
}

// ============================================
// 3. 重试函数
// ============================================

/**
 * 创建带重试的函数
 * @param {Function} fn - 可能失败的异步函数
 * @param {number} times - 最大重试次数
 * @param {number} delay - 重试间隔（毫秒）
 * @returns {Function} 带重试的函数
 */
export function retry(fn, times, delay = 0) {
  // TODO: 返回一个新函数，失败时自动重试
}

// ============================================
// 4. 超时函数
// ============================================

/**
 * 给函数添加超时限制
 * @param {Function} fn - 异步函数
 * @param {number} ms - 超时时间（毫秒）
 * @returns {Function} 带超时的函数
 */
export function timeout(fn, ms) {
  // TODO: 返回一个新函数，超时则 reject
}

// ============================================
// 5. 批量处理
// ============================================

/**
 * 将数组分批处理
 * @param {Function} fn - 处理单个元素的函数
 * @param {number} size - 批量大小
 * @returns {Function} 批量处理函数
 */
export function batch(fn, size) {
  // TODO: 返回一个函数，接收数组，分批处理
  // 每批处理 size 个元素
}
