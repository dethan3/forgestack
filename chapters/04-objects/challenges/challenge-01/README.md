# 综合挑战 01：对象工具库

## 🎯 挑战目标

实现一组实用的对象处理工具函数。

## 📋 需求说明

实现 `objectUtils.js` 中的所有函数：

### 1. `get(obj, path, defaultValue)`
安全获取嵌套属性

```javascript
get({ a: { b: { c: 1 } } }, 'a.b.c')  // 1
get({ a: 1 }, 'a.b.c', 'default')     // 'default'
```

### 2. `set(obj, path, value)`
设置嵌套属性（自动创建路径）

```javascript
set({}, 'a.b.c', 1)  // { a: { b: { c: 1 } } }
```

### 3. `deepClone(obj)`
深度克隆对象

### 4. `isEqual(obj1, obj2)`
深度比较两个值是否相等

### 5. `merge(target, ...sources)`
深度合并多个对象

## ⏱️ 预计时间

45-60 分钟

## 🚀 开始挑战

1. 编辑 `objectUtils.js` 实现功能
2. 运行测试：`pnpm test chapters/04-objects/challenges`
3. 所有测试通过即为完成

## 💡 提示

- 路径解析可以用 split('.')
- 深度操作需要递归
- 注意处理数组和对象的区别
