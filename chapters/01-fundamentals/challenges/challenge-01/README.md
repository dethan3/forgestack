# 综合挑战 01：成绩管理系统

## 🎯 挑战目标

运用本章所学知识，实现一个简单的成绩管理系统。

## 📋 需求说明

实现 `gradeSystem.js` 中的所有函数：

### 1. `addStudent(students, name, scores)`
- 添加学生到学生列表
- `scores` 是一个分数数组
- 返回更新后的学生列表

### 2. `getAverage(student)`
- 计算单个学生的平均分
- 保留两位小数

### 3. `getGrade(average)`
- 根据平均分返回等级
- 90+: 'A', 80+: 'B', 70+: 'C', 60+: 'D', <60: 'F'

### 4. `getTopStudent(students)`
- 返回平均分最高的学生
- 如果列表为空，返回 null

### 5. `getStatistics(students)`
- 返回统计信息对象：
  - `total`: 学生总数
  - `average`: 全班平均分
  - `highest`: 最高平均分
  - `lowest`: 最低平均分

## ⏱️ 预计时间

30-45 分钟

## 🚀 开始挑战

1. 编辑 `gradeSystem.js` 实现功能
2. 运行测试：`pnpm test chapters/01-fundamentals/challenges`
3. 所有测试通过即为完成

## 💡 提示

- 使用 `const` 和 `let` 声明变量
- 使用 `for...of` 遍历数组
- 使用对象映射简化等级判断
- 注意边界情况处理
