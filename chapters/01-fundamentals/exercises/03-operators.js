/**
 * 练习 03：运算符
 *
 * 学习目标：
 * - 掌握比较运算符（=== vs ==）
 * - 理解逻辑运算符和短路求值
 * - 使用空值合并和可选链运算符
 */

// ============================================
// 练习 1：严格相等
// ============================================

/**
 * 使用严格相等比较两个值
 */
export function strictEqual(a, b) {
  // TODO: 使用 === 比较
}

// ============================================
// 练习 2：逻辑与短路
// ============================================

/**
 * 如果 condition 为真，返回 value，否则返回 null
 * 使用 && 短路求值实现
 */
export function andShortCircuit(condition, value) {
  // TODO: 使用 && 实现
}

// ============================================
// 练习 3：逻辑或默认值
// ============================================

/**
 * 返回 value，如果 value 是假值则返回 defaultValue
 * 使用 || 实现
 */
export function orDefault(value, defaultValue) {
  // TODO: 使用 || 实现
}

// ============================================
// 练习 4：空值合并
// ============================================

/**
 * 返回 value，如果 value 是 null 或 undefined 则返回 defaultValue
 * 使用 ?? 实现
 */
export function nullishDefault(value, defaultValue) {
  // TODO: 使用 ?? 实现
}

// ============================================
// 练习 5：可选链
// ============================================

/**
 * 安全获取用户的城市
 * user 可能是 null，或者没有 address 属性
 */
export function getUserCity(user) {
  // TODO: 使用可选链 ?. 获取 user.address.city
  // 如果不存在返回 undefined
}

// ============================================
// 练习 6：三元运算符
// ============================================

/**
 * 根据分数返回等级
 * >= 60 返回 'Pass'，否则返回 'Fail'
 */
export function getGrade(score) {
  // TODO: 使用三元运算符实现
}

// ============================================
// 练习 7：展开运算符 - 数组
// ============================================

/**
 * 合并两个数组
 */
export function mergeArrays(arr1, arr2) {
  // TODO: 使用展开运算符 ... 合并数组
}

// ============================================
// 练习 8：展开运算符 - 对象
// ============================================

/**
 * 合并两个对象，后者覆盖前者的同名属性
 */
export function mergeObjects(obj1, obj2) {
  // TODO: 使用展开运算符 ... 合并对象
}

// ============================================
// 练习 9：综合应用
// ============================================

/**
 * 获取用户显示名称
 * 优先级：user.displayName > user.name > 'Anonymous'
 * 注意：空字符串应该被视为无效值
 */
export function getDisplayName(user) {
  // TODO: 使用 || 或 ?? 实现
  // 提示：考虑空字符串的情况
}
