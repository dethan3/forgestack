/**
 * 练习 01：变量声明
 *
 * 学习目标：
 * - 理解 let、const、var 的区别
 * - 掌握块级作用域
 * - 遵循命名规范
 *
 * 说明：
 * - 每个函数都有注释说明要求
 * - 修改函数体内的代码，使测试通过
 * - 运行测试：pnpm test:01
 */

// ============================================
// 练习 1：使用 const 声明常量
// ============================================

/**
 * 返回圆周率 PI 的值
 * 要求：使用 const 声明
 */
export function getPI() {
  // TODO: 使用 const 声明 PI，值为 3.14159
}

// ============================================
// 练习 2：使用 let 声明可变变量
// ============================================

/**
 * 计算从 1 到 n 的累加和
 * 要求：使用 let 声明累加器
 */
export function sumToN(n) {
  // TODO: 使用 let 声明 sum，初始值为 0
  // TODO: 使用循环累加 1 到 n
  // TODO: 返回 sum
}

// ============================================
// 练习 3：块级作用域
// ============================================

/**
 * 演示块级作用域
 * 要求：在 if 块内声明变量，返回块外的变量值
 */
export function blockScope() {
  const outer = 'outer';

  if (true) {
    // TODO: 使用 const 声明 inner = 'inner'
    // 注意：inner 只在这个块内有效
  }

  // TODO: 返回 outer 的值
}

// ============================================
// 练习 4：const 与对象
// ============================================

/**
 * 修改对象属性
 * 要求：const 声明的对象，其属性可以修改
 */
export function modifyObject() {
  // TODO: 使用 const 声明 user 对象，包含 name: 'Alice'
  // TODO: 将 user.name 修改为 'Bob'
  // TODO: 返回 user
}

// ============================================
// 练习 5：变量命名规范
// ============================================

/**
 * 创建符合命名规范的变量
 * 返回一个对象，包含以下属性：
 * - userName: 字符串 'John'
 * - MAX_COUNT: 数字 100
 * - isActive: 布尔值 true
 */
export function namingConventions() {
  // TODO: 按照命名规范声明变量
  // TODO: 返回包含这些变量的对象
}

// ============================================
// 练习 6：解构赋值声明（预习）
// ============================================

/**
 * 使用解构赋值从对象中提取值
 */
export function destructuringBasics() {
  const person = { name: 'Alice', age: 25, city: 'Beijing' };

  // TODO: 使用解构赋值提取 name 和 age
  // const { name, age } = person;

  // TODO: 返回 `${name} is ${age} years old`
}
