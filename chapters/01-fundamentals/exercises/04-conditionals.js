/**
 * 练习 04：条件语句
 *
 * 学习目标：
 * - 掌握 if/else 语句
 * - 理解 switch 语句
 * - 应用真值/假值判断
 */

// ============================================
// 练习 1：基本 if/else
// ============================================

/**
 * 判断数字是正数、负数还是零
 * 返回 'positive', 'negative', 或 'zero'
 */
export function checkNumber(num) {
  // TODO: 使用 if/else if/else 实现
}

// ============================================
// 练习 2：多条件判断
// ============================================

/**
 * 根据分数返回等级
 * >= 90: 'A'
 * >= 80: 'B'
 * >= 70: 'C'
 * >= 60: 'D'
 * < 60: 'F'
 */
export function getLetterGrade(score) {
  // TODO: 使用 if/else if 实现
}

// ============================================
// 练习 3：switch 语句
// ============================================

/**
 * 根据星期几返回是否是工作日
 * 1-5: 'weekday'
 * 6-7: 'weekend'
 * 其他: 'invalid'
 */
export function getDayType(day) {
  // TODO: 使用 switch 实现
}

// ============================================
// 练习 4：真值判断
// ============================================

/**
 * 检查用户是否有效
 * 用户有效条件：user 存在且 user.name 非空
 */
export function isValidUser(user) {
  // TODO: 使用真值判断实现
}

// ============================================
// 练习 5：提前返回
// ============================================

/**
 * 计算数组元素的平均值
 * 如果数组为空或不是数组，返回 0
 */
export function average(arr) {
  // TODO: 使用提前返回处理边界情况
  // TODO: 计算并返回平均值
}

// ============================================
// 练习 6：对象映射代替 switch
// ============================================

/**
 * 根据状态码返回状态文本
 * 200: 'OK'
 * 201: 'Created'
 * 400: 'Bad Request'
 * 404: 'Not Found'
 * 500: 'Internal Server Error'
 * 其他: 'Unknown'
 */
export function getStatusText(code) {
  // TODO: 使用对象映射实现
}

// ============================================
// 练习 7：复合条件
// ============================================

/**
 * 检查是否可以投票
 * 条件：年龄 >= 18 且 是公民（isCitizen 为 true）
 */
export function canVote(age, isCitizen) {
  // TODO: 使用逻辑运算符组合条件
}

// ============================================
// 练习 8：嵌套条件简化
// ============================================

/**
 * 获取用户权限级别
 * - 如果不是用户，返回 'none'
 * - 如果是管理员（isAdmin），返回 'admin'
 * - 如果是版主（isModerator），返回 'moderator'
 * - 否则返回 'user'
 */
export function getPermissionLevel(user) {
  // TODO: 使用提前返回避免嵌套
}
