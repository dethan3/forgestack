# 综合挑战 01：数据处理器

## 🎯 挑战目标

实现一个数据处理工具库，综合运用字符串、数组和类型处理方法。

## 📋 需求说明

实现 `dataProcessor.js` 中的所有函数：

### 1. `parseCSV(csvString)`
- 解析 CSV 格式字符串为对象数组
- 第一行是表头，后续行是数据

```javascript
parseCSV('name,age\nAlice,25\nBob,30')
// => [{ name: 'Alice', age: '25' }, { name: 'Bob', age: '30' }]
```

### 2. `filterByField(data, field, value)`
- 筛选数组中指定字段等于指定值的项

### 3. `sortByField(data, field, order)`
- 按指定字段排序，order 为 'asc' 或 'desc'
- 支持数字和字符串排序

### 4. `groupByField(data, field)`
- 按指定字段分组

```javascript
groupByField([{type: 'a', v: 1}, {type: 'b', v: 2}, {type: 'a', v: 3}], 'type')
// => { a: [{type: 'a', v: 1}, {type: 'a', v: 3}], b: [{type: 'b', v: 2}] }
```

### 5. `summarize(data, field)`
- 统计数字字段的 count、sum、avg、min、max

## ⏱️ 预计时间

45-60 分钟

## 🚀 开始挑战

1. 编辑 `dataProcessor.js` 实现功能
2. 运行测试：`pnpm test chapters/02-data-types/challenges`
3. 所有测试通过即为完成

## 💡 提示

- CSV 解析可以用 split 分割行和列
- 排序时注意数字和字符串的比较方式不同
- 分组可以用 reduce 实现
