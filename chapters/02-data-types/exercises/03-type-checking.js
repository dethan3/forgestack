/**
 * 练习 03：类型检测与转换
 *
 * 学习目标：
 * - 掌握类型检测方法
 * - 理解类型转换规则
 */

// ============================================
// 练习 1：精确类型检测
// ============================================

/**
 * 返回值的精确类型
 * 'number', 'string', 'boolean', 'undefined',
 * 'null', 'array', 'object', 'function', 'symbol', 'bigint'
 */
export function getType(value) {
  // TODO: 处理 null、数组等特殊情况
}

// ============================================
// 练习 2：检测空值
// ============================================

/**
 * 检测值是否为空（null, undefined, '', [], {}）
 */
export function isEmpty(value) {
  // TODO: 检测各种空值情况
}

// ============================================
// 练习 3：安全转数字
// ============================================

/**
 * 安全地将值转换为数字
 * 无法转换时返回默认值
 */
export function toNumber(value, defaultValue = 0) {
  // TODO: 转换并处理 NaN
}

// ============================================
// 练习 4：深度相等
// ============================================

/**
 * 检测两个值是否深度相等
 * 支持对象和数组的深度比较
 */
export function deepEqual(a, b) {
  // TODO: 实现深度比较
  // 提示：递归比较对象属性
}

// ============================================
// 练习 5：类型守卫
// ============================================

/**
 * 如果值是数组则返回，否则包装成数组
 * ensureArray(1) => [1]
 * ensureArray([1, 2]) => [1, 2]
 * ensureArray(null) => []
 */
export function ensureArray(value) {
  // TODO: 使用 Array.isArray
}

// ============================================
// 练习 6：安全属性访问
// ============================================

/**
 * 安全地获取嵌套属性
 * get({ a: { b: 1 } }, 'a.b') => 1
 * get({ a: 1 }, 'a.b.c', 'default') => 'default'
 */
export function get(obj, path, defaultValue = undefined) {
  // TODO: 解析路径并逐层访问
}

// ============================================
// 练习 7：克隆对象
// ============================================

/**
 * 深度克隆对象/数组
 */
export function deepClone(value) {
  // TODO: 递归克隆
  // 提示：处理数组和普通对象
}

// ============================================
// 练习 8：合并对象
// ============================================

/**
 * 深度合并多个对象
 * deepMerge({ a: 1 }, { b: 2 }) => { a: 1, b: 2 }
 * deepMerge({ a: { x: 1 } }, { a: { y: 2 } }) => { a: { x: 1, y: 2 } }
 */
export function deepMerge(...objects) {
  // TODO: 递归合并对象
}
