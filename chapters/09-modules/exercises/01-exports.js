/**
 * 练习 01：导出
 *
 * 学习目标：
 * - 命名导出
 * - 默认导出
 * - 重命名导出
 */

// ============================================
// 练习 1：命名导出
// ============================================

/**
 * 导出常量 PI
 * 值为 3.14159
 */
// TODO: export const PI = ...

/**
 * 导出函数 add
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
// TODO: export function add(a, b) { ... }

/**
 * 导出函数 subtract
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
// TODO: export function subtract(a, b) { ... }

/**
 * 导出函数 multiply
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
// TODO: export function multiply(a, b) { ... }

/**
 * 导出函数 divide
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
// TODO: export function divide(a, b) { ... }

// ============================================
// 练习 2：类导出
// ============================================

/**
 * 导出 Calculator 类
 * 包含方法: add, subtract, multiply, divide
 */
// TODO: export class Calculator { ... }

/**
 * 导出 Counter 类
 * 包含: count 属性, increment(), decrement(), reset() 方法
 */
// TODO: export class Counter { ... }

// ============================================
// 练习 3：对象和数组导出
// ============================================

/**
 * 导出配置对象 config
 * 包含: apiUrl, timeout, retries
 */
// TODO: export const config = { ... }

/**
 * 导出颜色数组 colors
 * 包含: 'red', 'green', 'blue'
 */
// TODO: export const colors = [ ... ]

// ============================================
// 练习 4：默认导出
// ============================================

/**
 * 默认导出 MathUtils 对象
 * 包含所有数学函数: add, subtract, multiply, divide, PI
 */
// TODO: export default { ... }
