/**
 * 数组工具模块
 */

/**
 * 数组去重
 * @param {Array} arr
 * @returns {Array}
 */
export function unique(arr) {
  // TODO: 实现
}

/**
 * 数组扁平化
 * @param {Array} arr
 * @param {number} depth - 扁平化深度，默认 Infinity
 * @returns {Array}
 */
export function flatten(arr, depth = Infinity) {
  // TODO: 实现
}

/**
 * 数组分块
 * @param {Array} arr
 * @param {number} size - 每块大小
 * @returns {Array<Array>}
 */
export function chunk(arr, size) {
  // TODO: 实现
}

/**
 * 打乱数组
 * @param {Array} arr
 * @returns {Array} - 返回新数组
 */
export function shuffle(arr) {
  // TODO: 实现 Fisher-Yates 算法
}
